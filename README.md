# olx-panel

Panel do zarządzania linkami obserwowanymi przez worker `olx-monitor`.
Statyczna strona (GitHub Pages) — dodaje/usuwa wyszukiwania przez endpoint
`/targets` workera. Token panelu trzymany jest w localStorage przeglądarki.
